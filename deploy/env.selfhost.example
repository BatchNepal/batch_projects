# Copy to .env in this directory, fill in real values, then:
#   docker compose -f docker-compose.selfhost.yml up -d

MARIADB_ROOT_PASSWORD=changeme-use-a-real-password
ADMIN_PASSWORD=changeme-use-a-real-password
SITE_NAME=erp.localhost
ERPNEXT_VERSION=v15.44.0
HTTP_PORT=8080
