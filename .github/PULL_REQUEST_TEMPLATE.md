**What changed and why** (not just what — the reviewer can read the diff for that)

**Which `version-NN` branch is this against, and did you test on that ERPNext version?**

**Screenshots** (if this touches the UI)

**Checklist**

- [ ] If `frontend/src/` changed, I ran `yarn build` and committed the result under `batch_projects/public/frontend/`
- [ ] CI passes
- [ ] I haven't touched anything under the (gitignored) internal docs/planning paths
